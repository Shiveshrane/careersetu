# ── Kokoro TTS Backend Environment ──

# Device: "cuda" for GPU (auto-falls back to CPU if CUDA unavailable)
KOKORO_DEVICE=cuda

# Language code for Kokoro pipeline
KOKORO_LANG_CODE=a

# Regex to split text into sentences for streaming
KOKORO_SPLIT_PATTERN=(?<=[.!?])\s+

# Server port
PORT=8001
